# Python Best Practices

When writing Python code for HELIOS:
1. Always use type hints (e.g. `def my_func(a: int) -> str:`).
2. Avoid deeply nested logic; use early returns.
3. Use modern language features from Python 3.10+ (like `match`/`case`).
4. Write docstrings for all public functions and classes.
